# SourcingOS V2 闪电开发执行手册 (最终版)

**作者**: Manus AI  
**日期**: 2026年2月5日  
**目标**: 24小时内完成"数字化作战大屏 + AI 询盘解析 + 园区工厂匹配"的原型级 Demo

---

## 📋 开发前检查清单

在明天 09:00 准时开始开发前，请确认以下事项：

### 环境准备
- [ ] Directus 后端（47.99.205.136:8055）处于运行状态
- [ ] 已获取最新的 Directus API Token（或使用账号 magic@gmail.com / wysk1214 登录）
- [ ] GitHub 仓库 `magicy565-web/demand-os` 已准备好接收新代码
- [ ] 本地开发环境已配置（Node.js 22.13.0 + npm/pnpm）

### 数据准备
- [ ] 已准备好 5-10 个真实或模拟的产业带数据（见 `industrial_belt_data.md`）
- [ ] 已准备好 10-15 个园区工厂的样本数据（名称、主营品类、MOQ、交期等）
- [ ] 已准备好 2-3 个示例 Excel 询价表（用于演示 AI 解析功能）

### 技术资源
- [ ] 已获取优化后的中国地图 SVG 路径数据（或使用开源资源）
- [ ] 已确认 Framer Motion、Tailwind CSS 等依赖已安装

---

## 🚀 Day 1 开发任务时间表

### 时间段 1: 09:00 - 11:00 | 后端配置与地图基础 (2小时)

**目标**: 完成 Directus Collections 配置，前端地图基础渲染。

#### 后端任务 (Directus)
1.  **创建 `industrial_belts` Collection**
    *   字段：`id`, `name`, `province`, `coordinates` (JSON), `svg_path_id`, `core_products`, `advantages`, `factory_count`, `status`
    *   权限：Public 可读，已认证用户可写

2.  **创建 `factories` Collection**
    *   字段：`id`, `name`, `industrial_belt_id` (关联), `main_products`, `moq`, `lead_time`, `certifications`, `status`

3.  **创建 `rfqs` Collection** (询盘表)
    *   字段：`id`, `uploaded_at`, `file_url`, `parsed_data` (JSON), `matched_factories` (多对多), `status`

4.  **数据填充**：插入 5 个核心产业带和 10 个示例工厂

#### 前端任务
1.  **创建 `/web/src/components/ChinaIndustrialMap.tsx`**
    *   导入优化的中国地图 SVG 路径
    *   渲染基础地图轮廓（深蓝色背景，灰色省份边界）
    *   从 Directus 获取 `industrial_belts` 数据

2.  **创建 `/web/src/components/IndustrialBeltMarker.tsx`**
    *   为每个产业带渲染发光脉冲点
    *   基础的鼠标悬停效果（缩放 + 边框高亮）

3.  **创建 `/web/src/app/page.tsx` (首页)**
    *   集成 `ChinaIndustrialMap` 组件
    *   设置暗色系背景和科技感主题

**关键代码片段**：
```typescript
// /web/src/lib/directus.ts 扩展
export async function getIndustrialBelts() {
  return await directus.items('industrial_belts').readByQuery({
    sort: ['name'],
  });
}

export async function getFactories(industrialBeltId: string) {
  return await directus.items('factories').readByQuery({
    filter: { industrial_belt_id: { _eq: industrialBeltId } },
  });
}
```

---

### 时间段 2: 11:00 - 13:00 | 作战大屏动效与交互 (2小时)

**目标**: 完成地图的科技感动效，实现产业带悬停交互。

#### 任务清单
1.  **背景光流动画**
    *   使用 Framer Motion 或 CSS 实现缓慢的粒子流或渐变动画
    *   增加 `<defs><filter>` 中的 `feGaussianBlur` 和 `feDropShadow` 滤镜

2.  **产业带脉冲动画**
    *   为 `IndustrialBeltMarker` 中的圆形节点添加 Framer Motion 的 `animate` 属性
    *   实现周期性的发光脉冲（opacity 和 r 属性变化）

3.  **悬停信息卡片**
    *   创建 `/web/src/components/IndustrialBeltTooltip.tsx`
    *   显示产业带名称、核心优势、工厂数量、代表品类
    *   使用 Framer Motion 的 `AnimatePresence` 实现平滑进出动画

4.  **连接线光流**
    *   在地图上绘制从产业带向外辐射的 SVG 线条
    *   使用 `strokeDasharray` 和 `strokeDashoffset` 实现流动效果

**关键代码片段**：
```typescript
// /web/src/components/IndustrialBeltMarker.tsx
import { motion } from 'framer-motion';

export default function IndustrialBeltMarker({ belt }: IndustrialBeltMarkerProps) {
  const [isHovered, setIsHovered] = useState(false);

  return (
    <motion.g
      onMouseEnter={() => setIsHovered(true)}
      onMouseLeave={() => setIsHovered(false)}
      className="cursor-pointer"
    >
      {/* 脉冲圆形 */}
      <motion.circle
        cx={belt.coordinates.lng}
        cy={belt.coordinates.lat}
        r={5}
        className="fill-industrial-highlight"
        animate={{ r: [5, 8, 5], opacity: [1, 0.6, 1] }}
        transition={{ duration: 2, repeat: Infinity }}
      />
      {/* 悬停时显示卡片 */}
      <AnimatePresence>
        {isHovered && (
          <motion.g initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}>
            {/* 卡片 SVG 或 HTML overlay */}
          </motion.g>
        )}
      </AnimatePresence>
    </motion.g>
  );
}
```

---

### 时间段 3: 14:00 - 16:00 | AI 询盘解析 UI 实现 (2小时)

**目标**: 完成 Module 01 的文件上传和 AI 解析结果展示。

#### 任务清单
1.  **创建 `/web/src/components/RFQUploadForm.tsx`**
    *   文件上传区域（支持 Excel、PDF）
    *   拖拽上传支持
    *   上传进度条

2.  **创建 `/web/src/components/RFQParsingResult.tsx`**
    *   展示 AI 解析后的结构化物料清单
    *   表格形式：产品名称、规格、数量、目标价、材质等
    *   编辑功能（允许用户修改解析结果）

3.  **创建 `/web/src/app/industrial-os/rfq/page.tsx`**
    *   集成上传表单和解析结果展示
    *   实现文件上传 → AI 解析 → 结果展示的流程

4.  **AI 解析逻辑**（简化版，用于演示）
    *   在 Demo 阶段，使用预定义的解析规则或模拟数据
    *   实际项目中可集成 OpenAI API 进行真实解析

**关键代码片段**：
```typescript
// /web/src/lib/rfq-parser.ts (模拟 AI 解析)
export function parseRFQData(fileContent: string): ParsedRFQ {
  // 简化的解析逻辑，实际项目中应调用 LLM API
  const mockParsedData = {
    products: [
      { name: 'Bed Frames', quantity: 200, targetPrice: 50, material: 'Wood' },
      { name: 'Nightstands', quantity: 400, targetPrice: 20, material: 'Wood' },
      // ...
    ],
  };
  return mockParsedData;
}
```

---

### 时间段 4: 16:00 - 18:00 | 工厂匹配系统 (2小时)

**目标**: 完成根据解析结果自动匹配园区工厂的逻辑。

#### 任务清单
1.  **创建 `/web/src/lib/factory-matching.ts`**
    *   根据解析的产品类型，从 Directus `factories` 表中筛选匹配的工厂
    *   评分逻辑：MOQ 匹配度、交期、认证资质等
    *   返回 Top 3 工厂

2.  **创建 `/web/src/components/FactoryMatchingResult.tsx`**
    *   展示匹配到的 3 家工厂卡片
    *   每张卡片显示：工厂名称、主营品类、MOQ、交期、认证、匹配度评分
    *   "联系工厂"按钮

3.  **创建 `/web/src/app/industrial-os/matching/page.tsx`**
    *   集成工厂匹配结果展示
    *   提供"导出报告"功能（生成 PDF 或 Excel）

**关键代码片段**：
```typescript
// /web/src/lib/factory-matching.ts
export async function matchFactories(parsedRFQ: ParsedRFQ): Promise<FactoryMatch[]> {
  const factories = await getFactories(); // 从 Directus 获取所有工厂
  
  const matches = factories
    .map(factory => ({
      factory,
      score: calculateMatchScore(factory, parsedRFQ),
    }))
    .sort((a, b) => b.score - a.score)
    .slice(0, 3);
  
  return matches;
}

function calculateMatchScore(factory: Factory, rfq: ParsedRFQ): number {
  // 简化的评分逻辑
  let score = 0;
  rfq.products.forEach(product => {
    if (factory.main_products.includes(product.name)) score += 30;
    if (factory.moq <= product.quantity) score += 20;
  });
  return score;
}
```

---

### 时间段 5: 18:00 - 20:00 | 全链路打通与演示优化 (2小时)

**目标**: 确保从地图点击 → 上传询盘 → 匹配工厂 → 导出报告的完整流程。

#### 任务清单
1.  **页面导航集成**
    *   从首页地图点击产业带 → 跳转到 RFQ 上传页面
    *   从 RFQ 上传页面 → 自动进入工厂匹配页面
    *   从工厂匹配页面 → 可导出报告或返回首页

2.  **页面过渡动画**
    *   使用 Framer Motion 的 `AnimatePresence` 和 `layoutId` 实现平滑的页面切换
    *   确保整个流程的视觉连贯性

3.  **演示数据优化**
    *   确保所有示例数据都是真实感强、符合行业背景的
    *   准备 2-3 套完整的演示场景（如"酒店采购"、"电子产品采购"等）

4.  **错误处理与加载状态**
    *   为所有 API 调用添加加载状态和错误提示
    *   确保用户体验流畅

5.  **响应式设计检查**
    *   在不同屏幕尺寸下测试（桌面、平板、手机）
    *   确保地图和表单在各设备上都能正常显示

---

## 🎬 演示脚本（3分钟完整演示）

为了确保明天的演示效果最佳，请按以下脚本进行：

1.  **打开首页** (0:00 - 0:30)
    *   展示中国产业带数字化作战大屏
    *   鼠标悬停在几个产业带，展示信息卡片和动效

2.  **进入 RFQ 上传** (0:30 - 1:00)
    *   点击某个产业带（如佛山），进入 RFQ 上传页面
    *   上传示例 Excel 询价表

3.  **AI 解析结果** (1:00 - 1:30)
    *   展示 AI 解析后的结构化物料清单
    *   演示编辑功能

4.  **工厂匹配** (1:30 - 2:30)
    *   展示自动匹配到的 3 家工厂
    *   强调匹配度评分和工厂优势

5.  **导出报告** (2:30 - 3:00)
    *   生成并下载匹配报告
    *   展示报告内容

---

## ⚠️ 开发中需要注意的事项

1.  **Directus API 调用**：确保所有 API 调用都包含正确的 Token 和错误处理。
2.  **SVG 性能**：如果地图 SVG 过大，使用 `<image>` 或 Canvas 进行优化。
3.  **动画性能**：避免过多的同时动画，使用 `will-change` CSS 优化。
4.  **数据一致性**：确保前后端数据同步，特别是产业带和工厂的关联关系。
5.  **用户反馈**：为所有交互添加视觉反馈（按钮按下、加载动画等）。

---

## 📦 交付物清单

在 Day 1 结束时，应完成以下交付物：

- [ ] 完整的 Directus 后端配置（Collections、数据、权限）
- [ ] 前端代码（所有组件、页面、工具函数）
- [ ] 完整的用户流程演示（从地图到工厂匹配）
- [ ] 响应式设计验证
- [ ] 演示脚本和截图

---

## 🎯 Day 2 调试与优化计划

Day 2 将专注于以下工作：

1.  **Bug 修复**：修复 Day 1 开发过程中发现的所有问题。
2.  **性能优化**：优化地图渲染、API 调用和动画性能。
3.  **视觉细节**：微调颜色、字体、间距等视觉元素。
4.  **GIF 录制**：录制 4 段高清演示 GIF（地图、上传、解析、匹配）。
5.  **文档完善**：编写最终的部署和使用文档。

---

## 🚀 准备好了吗？

这份手册包含了明天 24 小时开发的所有必要信息。请确保在 09:00 准时开始，按照时间表逐步推进。

**祝您开发顺利！**
