# 快速参考指南 - 本地开发域名访问

## 🎯 一分钟快速修复

### 对于本地开发:

**第一步** - 以管理员身份运行脚本:
```powershell
cd d:\Demand-os-v4
powershell -ExecutionPolicy Bypass -File setup-hosts.ps1
```

**第二步** - 启动前端:
```bash
cd d:\Demand-os-v4\web
npm run dev
```

**第三步** - 访问:
```
✓ http://localhost:3000
✓ http://saas.cnsubscribe.com:3000
✓ http://saas.cnsubscribe.xyz:3000
```

---

## 📋 关键概念理解

### 本地开发 vs 生产部署

```
┌──────────────────┬──────────────────────────┬────────────────────┐
│ 项目             │ 本地开发                 │ 生产部署            │
├──────────────────┼──────────────────────────┼────────────────────┤
│ 域名配置         │ Hosts 文件               │ 阿里云 DNS          │
│ 访问地址         │ localhost:3000           │ saas.cnsubscribe... │
│ 前端服务         │ npm run dev              │ Next.js build       │
│ API 后端         │ admin.cnsubscribe.xyz   │ saas.cnsubscribe... │
│ SSL/TLS          │ 不需要                   │ 需要有效证书        │
└──────────────────┴──────────────────────────┴────────────────────┘
```

### 为什么用 Hosts 文件?

**优点:**
- ✅ 简单快速，不需要修改阿里云 DNS
- ✅ 只影响本地，不影响其他用户
- ✅ 可以随时修改和回滚
- ✅ 完全免费

**缺点:**
- ❌ 只有本机可以访问
- ❌ 无法用于生产环境
- ❌ 需要管理员权限

---

## 🔧 常用命令

```bash
# 查看 hosts 文件
type C:\Windows\System32\drivers\etc\hosts

# 搜索特定条目
type C:\Windows\System32\drivers\etc\hosts | findstr saas

# 清空 DNS 缓存
ipconfig /flushdns

# 测试 DNS 解析
nslookup saas.cnsubscribe.com

# Ping 测试
ping saas.cnsubscribe.com

# 启动前端
npm run dev

# 测试前端 API 配置
curl http://localhost:3000/api/config-test
```

---

## ❌ 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 修改 hosts 后无法访问 | DNS 缓存 | 运行 `ipconfig /flushdns` |
| 提示权限被拒绝 | 非管理员 | 用管理员身份运行脚本 |
| 浏览器仍显示缓存 | 浏览器缓存 | Ctrl+Shift+Delete 清空 |
| API 连接失败 | 环境变量错误 | 检查 .env.local 文件 |
| saas.cnsubscribe.com:3000 无法访问 | Hosts 配置错误 | 重新运行 setup-hosts.ps1 |

---

## 📂 相关文件

```
d:\Demand-os-v4\
├── setup-hosts.ps1          ← 自动化修改 hosts 脚本 (PowerShell)
├── setup-hosts.bat          ← 自动化修改 hosts 脚本 (批处理)
├── QUICK_FIX_GUIDE.md       ← 详细修复指南
├── DNS_CONFIGURATION_GUIDE.md ← DNS 配置详解
├── DEPLOYMENT_GUIDE.md      ← 生产部署指南
└── web/
    ├── .env.local           ← 开发环境配置
    ├── .env.production      ← 生产环境配置
    └── next.config.ts       ← Next.js 配置
```

---

## 🚀 下一步

- [ ] 运行 `setup-hosts.ps1` 修改 hosts
- [ ] 启动 `npm run dev`
- [ ] 访问 `http://localhost:3000`
- [ ] 测试 `/api/config-test` 端点
- [ ] 验证 showroom 页面正常显示

---

## 📞 需要帮助?

1. 查看 `QUICK_FIX_GUIDE.md` - 完整修复指南
2. 查看 `DNS_CONFIGURATION_GUIDE.md` - 技术细节
3. 运行 `test-connectivity.ps1` - 诊断网络连接

---
